events {}
http {
    server {
        listen 80;
        location / {
            proxy_pass http://app:8080; # Đẩy traffic sang Tomcat
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}